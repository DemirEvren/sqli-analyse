apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  BACKEND_PORT: "3001"
  CORS_ORIGIN: "http://localhost,http://localhost:5173,http://localhost:3001,http://shelfware.local:8080,http://shelfware.local"