apiVersion: v1
kind: Service
metadata:
  name: kubecost
  labels:
    app: kubecost
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    protocol: TCP
    name: http
  selector:
    app: kubecost
