apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keda

# Install KEDA via Helm with minimal config
helmCharts:
  - name: keda
    repo: https://kedacore.github.io/charts
    version: 2.15.0
    releaseName: keda
    namespace: keda
